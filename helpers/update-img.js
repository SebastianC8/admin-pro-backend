const User = require('../models/user')
const Hospital = require('../models/hospital')
const Doctor = require('../models/doctor')

const updateImage = (type, id, path, filename) => {
    switch (type) {
        case value:
            
            break;
    
        default:
            break;
    }
}

module.exports = {
    updateImage
}